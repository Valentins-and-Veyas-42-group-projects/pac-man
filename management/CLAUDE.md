# Claude Code: project management

Follow [AGENTS.md](AGENTS.md) in this directory for the full
GitHub Issues/Project workflow (checking issues before starting work,
closing them on completion, opening new ones for discovered work, and
refreshing the snapshot table).

Claude-specific notes:

- This is 42 curriculum work: don't write game logic (maze
  generation, movement, collision, scoring, rendering) unprompted --
  the top-level `~/Projects/42/CLAUDE.md` rule about defending every
  line in evaluations applies here. Boilerplate/stubs/structure are
  fine; ask before filling in a stub with real logic unless
  explicitly asked to.
- When asked to update project-management state, edit
  [AGENTS.md](AGENTS.md)'s snapshot table (both files should stay in
  sync -- this file intentionally doesn't duplicate the table).
