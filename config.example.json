{
    # highscore_filename: where the persistent top-10 list is stored
    "highscore_filename": "highscores.json",

    # levels: one entry per level; width/height of the generated maze
    "levels": [
        { "width": 21, "height": 21 }
    ],

    "lives": 3,
    "pacgum": 42,
    "points_per_pacgum": 10,
    "points_per_super_pacgum": 50,
    "points_per_ghost": 200,

    # seed: used for the first level's maze (subsequent levels are random)
    "seed": 42,
    "level_max_time": 90
}
